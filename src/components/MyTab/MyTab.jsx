// import TicketsList from "../TicketsList";
import React from "react";
import "./MyTab.module.css";
import { Tabs } from "antd";

const MyTab = () => {
  // const onChange = (e) => {
  //   setSize(e.target.value);
  // };
  return (
    <div>
      {/* <Tabs
        defaultActiveKey="1"
        type="card"
        size={16}
        items={new Array(3).fill(null).map((_, i) => {
          const id = String(i + 1);
          return {
            label: `Card Tab ${id}`,
            key: id,
            children: [],
          };
        })}
      /> */}
    </div>
  );
};
export default MyTab;
